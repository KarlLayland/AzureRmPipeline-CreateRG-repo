# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Subscription'
    azureResourceManagerConnection: 'AzureRmPipeline-conn'
    subscriptionId: '1536499a-91e7-4e7d-b193-c1cbad71d769'
    location: 'UK South'
    templateLocation: 'URL of the file'
    csmFileLink: 'https://raw.githubusercontent.com/KarlLayland/AzureRmPipeline-CreateRG-repo/main/AzureRmPipeline-CreateRG/template.json'
    csmParametersFileLink: 'https://raw.githubusercontent.com/KarlLayland/AzureRmPipeline-CreateRG-repo/main/AzureRmPipeline-CreateRG/parameters.json'
    deploymentMode: 'Incremental'
    deploymentName: 'DeploymentPipelineKL'
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |

  displayName: 'Run a multi-line script'
